name: web_socket_analysis_server
description: A Dart web socket server running the analysis server.
# version: 0.1.0
# homepage: https://www.example.com
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  functions_framework: ^0.4.0
  shelf: ^1.0.0
  shelf_web_socket: ^1.0.1
  web_socket_channel: ^2.1.0

dev_dependencies:
  lints: ^1.0.0
  build_runner: ^2.0.0
  functions_framework_builder: ^0.4.0
  test: ^1.15.7
  test_process: ^2.0.0
